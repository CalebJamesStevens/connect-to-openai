const assert = require('assert');
const vscode = require('vscode');
const entension = require('../../extension');

suite('Extension Test Suite', () => {
	test('Pastes response when successful', () => {})
  test('Handles errors correctly', () => {})
  test('Shows error message when apiKey is invalid', () => {})
});
